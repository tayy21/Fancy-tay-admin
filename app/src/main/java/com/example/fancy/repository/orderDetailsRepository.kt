/*
package com.example.fancy.repository

import com.google.firebase.database.DatabaseReference
import com.google.firebase.database.ktx.database
import com.google.firebase.ktx.Firebase
import com.example.fancy.data.OrderDetails

class orderDetailsRepository {
    private val database: DatabaseReference = Firebase.database.reference.child("orderDetails")

    fun addOrderDetails(orderDetails: OrderDetails) {
        database.child(orderDetails.orderDetailsId).setValue(orderDetails)
    }



}

 */

